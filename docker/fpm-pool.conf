; Light FPM pool: few static workers, no TCP hop (nginx uses 127.0.0.1:9000).
[www]
pm = dynamic
pm.max_children = 12
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 4
pm.max_requests = 500
request_terminate_timeout = 180s
clear_env = no
